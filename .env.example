# TUM credentials (used by Artemis + Moodle SSO)
TUM_USERNAME=go42tum
TUM_PASSWORD=

# TUMonline personal calendar ICS export URL
# TUMonline → Kalender → Persönlicher Kalender → iCal-Export
TUM_CALENDAR=https://campus.tum.de/tumonline/...

# Anthropic API key (claude-sonnet-4-6 for agent/planner, claude-haiku for summaries/topics)
ANTHROPIC_API_KEY=sk-ant-...

# Optional: set to "true" for verbose HTTP + agent logging
DEBUG=false
